# firmware/arp

PlatformIO project for the generative arpeggiator firmware.

**Empty pending [Story 002](../../docs/stories/002-xiao-blinks.md)** — the first firmware PR will land `platformio.ini` (Seeed platform fork, `seeed_xiao_ra4m1` board), a minimal `src/main.cpp` blinky, and confirm the end-to-end toolchain works (install → compile → flash → LED blinks).

See [`../../docs/decisions.md`](../../docs/decisions.md) §2 for toolchain rationale and [`../../docs/stories/`](../../docs/stories/) for the full development backlog.
