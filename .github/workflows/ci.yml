name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Host unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
          key: ${{ runner.os }}-pio-${{ hashFiles('**/platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Run host tests (firmware/arp)
        run: |
          if [ -f firmware/arp/platformio.ini ]; then
            pio test -d firmware/arp -e native
          else
            echo "firmware/arp/platformio.ini not present yet — Story 002 will add it."
            echo "CI scaffolding verified: workflow runs, PlatformIO installs, matrix job completes."
          fi
